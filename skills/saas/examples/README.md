# Examples

The roadmap's running example is a construction-quotes SaaS (roadmap §3):
customers, projects, quotations, PDF generation, email, admin dashboard,
subscription billing — see `packages/saas-factory/src/crud.ts` for the
CRUD generator that produces the per-entity routes/pages for that kind of
brief.
