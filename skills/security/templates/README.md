`packages/security/src/scanSecrets.ts` — a real, versioned package (not a
copy-paste snippet), since this is core factory infrastructure every
project's quality gate should run through, not something dropped into a
generated project's own source tree.
